// Package views holds the templ templates and their generated code. Run
// `make generate` after editing any .templ file; the *_templ.go outputs are
// committed so the Docker build needs only the Go toolchain.
package views

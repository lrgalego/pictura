# Live data and local state — the VM is authoritative, never the checkout.
data/
bin/
coverage.out
coverage.html
# The 1Password service-account token — never committed; the engine refuses
# to run if this is tracked or un-ignored.
.op-token
# Local-only env fallback; the workflow is op run with the committed .tpl files.
.env
.direnv/
.DS_Store
result
result-*
# Claude Code local state (settings, worktrees)
.claude/

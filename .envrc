use flake

# cloudflared origin cert for this project's zone, so several projects on one
# machine keep their own certs instead of swapping ~/.cloudflared/cert.pem.
export TUNNEL_ORIGIN_CERT=$HOME/.cloudflared/cert.pictura.pem
